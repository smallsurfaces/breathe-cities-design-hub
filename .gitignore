node_modules/
dist/
.DS_Store
*.log

# Local Netlify folder
.netlify

# Next.js
.next/
out/

# TypeScript incremental build cache (generated by next build)
tsconfig.tsbuildinfo

# Environment files
.env*.local

# Claude agent context + internal logs — local-only, not shared via the repo
# (matches the other Breathe Cities repos; this repo is public)
CLAUDE.md
.claude/
